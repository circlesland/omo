<script lang="ts">
    // import { CirclesWrapper } from "../../kernel/circles-protocol/circlesActions";
    // import { navigateTo } from "../../kernel/viewRegistry";
    // let trustAddress = "";

    // async function trust() {
    //     if (trustAddress == "") return;
    //     let acc = CirclesWrapper.restoreAccount();
    //     let saf = await CirclesWrapper.getSafeAddress(acc);
    //     // CirclesWrapper.addTrustLine(acc, saf, trustAddress, 100);
    //     let untrusted = await CirclesWrapper.removeTrustLine(acc, saf, trustAddress);
    //     alert(JSON.stringify(untrusted));
    //     navigateTo("safe");
    // }
</script>
trust someone
<!-- 
<input placeholder="trust address" bind:value={trustAddress} />
<button on:click={()=>trust()}>trust</button> -->
